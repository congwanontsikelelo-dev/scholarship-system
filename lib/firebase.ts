// Minimal firebase placeholder. If you have Firebase config, replace this file with the real SDK initialization.

// Export placeholders so other modules can safely import from '@/lib/firebase'.
export const firebaseConfigured = false
export const auth: any = null
export const db: any = null

// If you later add Firebase, initialize and export `auth` and `db` from this module and set firebaseConfigured = true.
